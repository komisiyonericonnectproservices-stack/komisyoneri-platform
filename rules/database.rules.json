{
  "rules": {
    ".read": false,
    ".write": false,

    "properties": {
      ".read": true
    },
    "agents": {
      ".read": true
    },

    "adminAlerts": {
      "$alertId": {
        ".write": "auth != null && !data.exists()"
      }
    }
  }
}
